import React from 'react';
import useTitle from '../../../hooks/useTitle';

const Home = () => {

    return (
        <div>
            <h1>This is home page !!</h1>
        </div>
    );
};

export default Home;